# Optional override. On Vercel, AI Gateway auth is automatic.
# Locally, link the project and run `vercel env pull`.
CLEARPATH_MODEL=openai/gpt-5.4-mini

# Set to 1 to skip the model and run the corroboration rules only.
CLEARPATH_REHEARSAL=0
